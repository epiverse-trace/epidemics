# Specify no population change during an epidemic

Specify no population change during an epidemic

## Usage

``` r
.no_population_change(population)
```

## Arguments

- population:

  A `<population>` for which the population change list should be
  suitable.
