# Print a `<population>` object

Print a `<population>` object

## Usage

``` r
# S3 method for class 'population'
print(x, ...)
```

## Arguments

- x:

  A `<population>` object.

- ...:

  Other parameters passed to
  [`print()`](https://rdrr.io/r/base/print.html).

## Value

Invisibly returns the `<population>` object `x`. Called for printing
side-effects.
